---
import BitcoinLogo from '@/components/svg/BitcoinLogo.astro';
import { footerNav } from '@/data/navigation';
import { siteConfig } from '@/data/site';

const currentYear = new Date().getFullYear();
---

<footer class="border-t border-[var(--border-default)] bg-[var(--bg-secondary)]">
  <div class="container-wide py-12 md:py-16">
    <!-- Main footer content -->
    <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-8 lg:gap-12">
      <!-- Brand column -->
      <div class="col-span-2 md:col-span-4 lg:col-span-1">
        <a href="/" class="inline-flex items-center gap-2 font-semibold text-lg">
          <BitcoinLogo size={32} />
          <span>21dev.org</span>
        </a>
        <p class="mt-4 text-sm text-[var(--text-secondary)] max-w-xs">
          華語中文圈最全面的比特幣技術教育平台，推動比特幣技術在中文社群的發展。
        </p>
        <div class="mt-6 flex items-center gap-4">
          <!-- GitHub -->
          <a
            href={siteConfig.social.github}
            target="_blank"
            rel="noopener noreferrer"
            class="text-[var(--text-tertiary)] hover:text-[var(--text-primary)] transition-colors"
            aria-label="GitHub"
          >
            <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
              <path
                fill-rule="evenodd"
                d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"
                clip-rule="evenodd"
              />
            </svg>
          </a>
          <!-- Twitter/X -->
          <a
            href={siteConfig.social.twitter}
            target="_blank"
            rel="noopener noreferrer"
            class="text-[var(--text-tertiary)] hover:text-[var(--text-primary)] transition-colors"
            aria-label="Twitter"
          >
            <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z" />
            </svg>
          </a>
        </div>
      </div>

      <!-- Navigation columns -->
      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.resources.title}
        </h3>
        <ul class="space-y-3">
          {footerNav.resources.links.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.technology.title}
        </h3>
        <ul class="space-y-3">
          {footerNav.technology.links.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.community.title}
        </h3>
        <ul class="space-y-3">
          {footerNav.community.links.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.about.title}
        </h3>
        <ul class="space-y-3">
          {footerNav.about.links.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
                target={link.href.startsWith('http') ? '_blank' : undefined}
                rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="mt-12 pt-8 border-t border-[var(--border-default)]">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-sm text-[var(--text-tertiary)]">
          &copy; {currentYear} 21dev.org. 以 MIT 許可證開源。
        </p>
        <p class="text-sm text-[var(--text-tertiary)]">
          由社群驅動，為比特幣教育而生。
        </p>
      </div>
    </div>
  </div>
</footer>
