---
import BitcoinLogo from '@/components/svg/BitcoinLogo.astro';
import GitHubIcon from '@/components/svg/GitHubIcon.astro';
import TwitterIcon from '@/components/svg/TwitterIcon.astro';
import SignalIcon from '@/components/svg/SignalIcon.astro';
import { footerNav } from '@/data/navigation';
import { siteConfig } from '@/data/site';

const currentYear = new Date().getFullYear();
---

<footer class="border-t border-[var(--border-default)] bg-[var(--bg-secondary)]">
  <div class="container-wide py-12 md:py-16">
    <!-- Main footer content -->
    <div
      class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-6 sm:gap-8 lg:gap-12"
    >
      <!-- Brand column -->
      <div class="col-span-1 xs:col-span-2 md:col-span-4 lg:col-span-1">
        <a href="/" class="inline-flex items-center gap-2 font-semibold text-lg">
          <BitcoinLogo size={32} />
          <span>21dev.org</span>
        </a>
        <p class="mt-4 text-sm text-[var(--text-secondary)] max-w-xs">
          華語中文圈最全面的比特幣技術教育平台，推動比特幣技術在中文社群的發展。
        </p>
        <div class="mt-6 flex items-center gap-4">
          <!-- GitHub -->
          <a
            href={siteConfig.social.github}
            target="_blank"
            rel="noopener noreferrer"
            class="text-[var(--text-tertiary)] hover:text-[var(--text-primary)] transition-colors"
            aria-label="GitHub"
          >
            <GitHubIcon size={20} />
          </a>
          <!-- Twitter/X -->
          <a
            href={siteConfig.social.twitter}
            target="_blank"
            rel="noopener noreferrer"
            class="text-[var(--text-tertiary)] hover:text-[var(--text-primary)] transition-colors"
            aria-label="Twitter"
          >
            <TwitterIcon size={20} />
          </a>
          <!-- Signal -->
          <a
            href={siteConfig.social.signal}
            target="_blank"
            rel="noopener noreferrer"
            class="text-[var(--text-tertiary)] hover:text-[var(--text-primary)] transition-colors"
            aria-label="Signal"
          >
            <SignalIcon size={20} />
          </a>
        </div>
      </div>

      <!-- Navigation columns -->
      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.resources.title}
        </h3>
        <ul class="space-y-3">
          {
            footerNav.resources.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.technology.title}
        </h3>
        <ul class="space-y-3">
          {
            footerNav.technology.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.community.title}
        </h3>
        <ul class="space-y-3">
          {
            footerNav.community.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-4">
          {footerNav.about.title}
        </h3>
        <ul class="space-y-3">
          {
            footerNav.about.links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors"
                  target={link.href.startsWith('http') ? '_blank' : undefined}
                  rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="mt-12 pt-8 border-t border-[var(--border-default)]">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-sm text-[var(--text-tertiary)]">
          &copy; {currentYear} 21dev.org. 以 MIT 許可證開源。
        </p>
        <div class="flex items-center gap-4">
          <a
            href="/rss.xml"
            class="text-sm text-[var(--text-tertiary)] hover:text-bitcoin-500 transition-colors flex items-center gap-1"
            title="RSS Feed"
          >
            <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24">
              <path
                d="M6.18 15.64a2.18 2.18 0 0 1 2.18 2.18C8.36 19 7.38 20 6.18 20C5 20 4 19 4 17.82a2.18 2.18 0 0 1 2.18-2.18M4 4.44A15.56 15.56 0 0 1 19.56 20h-2.83A12.73 12.73 0 0 0 4 7.27V4.44m0 5.66a9.9 9.9 0 0 1 9.9 9.9h-2.83A7.07 7.07 0 0 0 4 12.93V10.1z"
              ></path>
            </svg>
            RSS
          </a>
          <p class="text-sm text-[var(--text-tertiary)]">由社群驅動，為比特幣教育而生。</p>
        </div>
      </div>
    </div>
  </div>
</footer>
